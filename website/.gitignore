# build artefacts
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# environment
.env
.env.*

# editor / OS
.DS_Store
.vscode/
.idea/

# generated by scripts/build-catalogs.mjs on every `npm run build` / `dev`.
# Not committed: they embed skill/source counts, so two PRs that both add a
# skill or source would collide on the same line for a value that is derived.
src/content/docs/skills/index.md
src/content/docs/sources/index.md
src/data/counts.json
